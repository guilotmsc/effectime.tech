json.extract! contract, :id, :employee_id, :client, :trading_name, :signature_date, :amount, :amount_ticket, :date_closing_hours, :payday, :email, :phone, :hours_volume, :attribuition_model, :tolerance, :created_at, :updated_at
json.url contract_url(contract, format: :json)
