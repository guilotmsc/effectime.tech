<%= form_with(model: corporation_type, local: true) do |form| %>
  <% if corporation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(corporation_type.errors.count, "error") %> prohibited this corporation_type from being saved:</h2>

      <ul>
      <% corporation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :corporation_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
