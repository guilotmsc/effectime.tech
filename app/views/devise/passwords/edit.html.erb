<div class="account-pages"></div>
<div class="clearfix"></div>
<div class="wrapper-page">
  <div class=" card-box">
    <div class="panel-heading">
      <h3 class="text-center"> Crie uma nova senha<strong class="text-custom"></strong> </h3>
    </div>

    <div class="panel-body">
      <%= form_for resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put, :class => 'form-horizontal m-t-20' } do |f| %>

        <%= devise_error_messages! %> 
 
        <div class="form-group">
          <div class="col-xs-12">
            <%= f.hidden_field :reset_password_token %>
            <%= f.password_field :password, autofocus: true, :class => "form-control", autocomplete: "off", placeholder: "Escolha uma senha" %>
            <% if @minimum_password_length %>
              <em>(Mínimo <%= @minimum_password_length %> caracteres)</em>
            <% end %><br />
          </div>
        </div>

        <div class="form-group ">
          <div class="col-xs-12">
            <%= f.password_field :password_confirmation, :class => "form-control", autocomplete: "off", placeholder: "Confirme sua senha" %>
          </div>
        </div>

        <div class="form-group text-center m-t-40">
          <div class="col-xs-12">
            <button class="btn btn-pink btn-block text-uppercase waves-effect waves-light" type="submit">
              Confirmar
            </button>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
