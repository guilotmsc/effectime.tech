<%= form_with(model: hour, local: true) do |form| %>
  <% if hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hour.errors.count, "error") %> prohibited this hour from being saved:</h2>

      <ul>
      <% hour.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :corporation_id %>
    <%= form.text_field :corporation_id, id: :hour_corporation_id %>
  </div>

  <div class="field">
    <%= form.label :volume %>
    <%= form.text_field :volume, id: :hour_volume %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
